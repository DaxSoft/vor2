{
  "name": "vor2",
  "private": true,
  "version": "0.1.0",
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @vor2/desktop tauri:dev",
    "desktop:run": "yarn workspace @vor2/desktop tauri:dev",
    "dev:web": "yarn workspace @vor2/desktop web:dev",
    "build": "turbo run build",
    "desktop:build": "yarn workspace @vor2/desktop tauri:build",
    "desktop:build:exe": "yarn workspace @vor2/desktop tauri:build:exe",
    "desktop:test": "yarn workspace @vor2/desktop test:desktop",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "prisma:generate": "yarn workspace @vor2/database prisma:generate",
    "prisma:migrate": "yarn workspace @vor2/database prisma:migrate"
  },
  "devDependencies": {
    "turbo": "^2.0.12"
  }
}
